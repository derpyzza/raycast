
    ✔ void DrawMap();
    ✔ void MovePlayer();
    void DrawRay2D();
    void CastRay2D();

    ✔ player x, player y, player direction(float)

    ✔ map cell size, map width, map height

    ✔ draw a line between the player, and the direction they're facing;

    ray x, ray y, ray direction(float), ray X offset, ray Y offset;

    draw dots at first intersection point to start off with
