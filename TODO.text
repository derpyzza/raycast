PLAYER{
    Position
    Direction
    fov
    MovePlayer();
}


RAY{
    Position
    Direction
    DrawRays();
    store rays in a struct
    --Find the x and y coordinates of the nearest horizontal line and the x offset and y offset;
    --Find the x and y coordinates of the nearest Vertical line and the x offset and y offset;
    --Find the shortest of the two;
    --Draw the shortest;
}